# Core application
ttkbootstrap>=1.10.1
PyYAML>=6.0
requests>=2.31.0

# Dataset access
datasets>=2.18.0
huggingface_hub>=0.23.0

# Local Hugging Face backend
transformers>=4.45.0
torch>=2.2.0
accelerate>=0.31.0
sentencepiece>=0.2.0
safetensors>=0.4.0

# Notes
# - tkinter is part of the standard Python install on Windows and macOS.
#   On Linux you may need the system package (for example: python3-tk).
